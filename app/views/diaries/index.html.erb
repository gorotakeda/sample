<p style="color :blue"><%= notice %></p>
<h2>日記</h2>
<% @diaries.each do |diary| %>
  <%= render diary %>
  <div>
  <%= link_to "詳細", diary %>
  </div>
</div>
<% end %>
<div>
<%= link_to "新しい日記を書く", new_diary_path %>
</div>
